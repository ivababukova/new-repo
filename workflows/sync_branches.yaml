name: Sync multiple branches
on:
  push:
    branches:
      - master
jobs:
  sync-branch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master

      - name: Merge master -> branch1
        uses: devmasx/merge-branch@master
        with:
          type: now
          from_branch: master
          target_branch: branch1
          github_token: ${{ github.token }}

